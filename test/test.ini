[db]
url = http://cevi.puzzle.ch/
defaultmail = tester@example.com

[file]
groupid = 42
headerlines = 4
footerlines = 0
freezecolumn = D

[rows]
# the ordering of these rows is swapped on purpose
c = id
B = first_name
a = last_name
d =
e =
F =
g = =SUM(D{row}:F{row})

